#include "tests.hpp"

// this is a compile-time library, so the behaviour goes into the .hpp files
int main() {
    // std::cout << typeid(type_var::value<big_recursion_test::b, RE>).name() << std::endl;
}